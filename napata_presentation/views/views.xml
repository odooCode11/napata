<odoo>
  <data>
    <!-- explicit list view definition -->
<!--
    <record model="ir.ui.view" id="napata_admission.list">
      <field name="name">napata_admission list</field>
      <field name="model">napata_admission.napata_admission</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
-->

    <!-- actions opening views on models -->
<!--
    <record model="ir.actions.act_window" id="napata_admission.action_window">
      <field name="name">napata_admission window</field>
      <field name="res_model">napata_admission.napata_admission</field>
      <field name="view_mode">tree,form</field>
    </record>
-->

    <!-- server action to the one above -->
<!--
    <record model="ir.actions.server" id="napata_admission.action_server">
      <field name="name">napata_admission server</field>
      <field name="model_id" ref="model_napata_admission_napata_admission"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": model._name,
        }
      </field>
    </record>
-->

    <!-- Top menu item -->
<!--
    <menuitem name="napata_admission" id="napata_admission.menu_root"/>
-->
    <!-- menu categories -->
<!--
    <menuitem name="Menu 1" id="napata_admission.menu_1" parent="napata_admission.menu_root"/>
    <menuitem name="Menu 2" id="napata_admission.menu_2" parent="napata_admission.menu_root"/>
-->
    <!-- actions -->
<!--
    <menuitem name="List" id="napata_admission.menu_1_list" parent="napata_admission.menu_1"
              action="napata_admission.action_window"/>
    <menuitem name="Server to list" id="napata_admission" parent="napata_admission.menu_2"
              action="napata_admission.action_server"/>
-->
  </data>
</odoo>